<div class="flex gap-3 flex-shrink-0 ml-6">
	<a href="{{ path('app_team_edit', {'id': team.id}) }}" class="btn-primary">Éditer</a>
	<button type="submit" form="delete-form" class="btn-primary bg-red-600 hover:bg-red-700">Supprimer</button>
</div>
